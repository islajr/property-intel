# Railway configuration for static SPA hosting

[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npx -y serve -s dist -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
